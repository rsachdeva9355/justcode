#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

#define ll long long

int main() {
	long n;
	ll l, r;
	cin >> n >> l >> r;
	vector<long> arr(n);
	for (long i = 0; i < n; i++) {
		cin >> arr[i];
	}
	sort(arr.begin(), arr.end());
	for (long i = 0; i < n; i++) {
		
	}
	return 0;
}